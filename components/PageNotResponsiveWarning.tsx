import Warning from "./Warning.tsx";

export default function PageNotResponsiveWarning() {
  return (
    <Warning text="Currently, this page is NOT responsive in mobile devices! Sorry, working
    on it!" />
  );
}
